name: test the actions

on:
  workflow-dispatch

jobs:
  print-hello-world:
    name: Print Hello world
    runs-on: ubuntu-latest
    steps: 
      - id: name
        run: |
          echo "This is my action"
